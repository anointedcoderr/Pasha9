node_modules
.next
.turbo
dist
build
out
*.log
.env
.env.local
.env.*.local
.DS_Store
Thumbs.db
.vscode/*
!.vscode/extensions.json
.idea
coverage
*.tsbuildinfo

# Nested clones of this repo (in case the project is checked out into itself)
/Sanjid14/

# Operator-uploaded media (banners, atelier images, sounds, etc.).
# Lives at UPLOAD_ROOT in production (/var/www/pasha9/uploads). The
# dev path falls back to ./uploads at the repo root.
/uploads/

# Local AI-assistant tool config. Never part of the shipped product.
.claude/
