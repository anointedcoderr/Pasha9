{
  "name": "@sanjid14/api",
  "version": "0.1.0",
  "private": true,
  "description": "Backend API for sanjid14. Implementation begins in Milestone 2.",
  "scripts": {
    "dev": "echo \"API runs in Milestone 2. See docs/milestones.md\" && exit 0",
    "build": "echo \"API runs in Milestone 2. See docs/milestones.md\" && exit 0",
    "start": "echo \"API runs in Milestone 2. See docs/milestones.md\" && exit 0",
    "lint": "echo \"\" && exit 0",
    "typecheck": "echo \"\" && exit 0"
  }
}
